apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: forgejo-cert
  namespace: default
spec:
  secretName: forgejo-cert
  commonName: FQDN
  duration: 2160h # 90d
  renewBefore: 360h # 15d
  dnsNames:
  - FQDN
  issuerRef:
    name: staging
    kind: ClusterIssuer