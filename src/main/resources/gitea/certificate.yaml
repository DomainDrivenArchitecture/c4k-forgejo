apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gitea-cert
  namespace: default
spec:
  secretName: gitea-secret
  commonName: FQDN
  duration: 2160h # 90d
  renewBefore: 360h # 15d
  dnsNames:
  - FQDN
  issuerRef:
    name: staging
    kind: ClusterIssuer